## 2024-05-22 - Accessibility in Icon-only Buttons
**Learning:** Icon-only buttons (like Keypad delete) often get missed during development but are critical for screen reader users.
**Action:** Always check `Keypad` and other iconic controls for `aria-label`.
