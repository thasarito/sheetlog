## 2026-02-18 - Mobile Keypad Interaction
**Learning:** For high-frequency interactive components like keypads, `touch-manipulation` and `select-none` are critical for preventing accidental zooming and text selection on mobile devices, making the app feel native.
**Action:** Always add `touch-manipulation` and `select-none` to custom button grids in PWAs.
