## 2025-02-14 - Keypad Accessibility
**Learning:** Icon-only buttons (like "DEL" in keypads) and punctuation keys (like ".") need explicit `aria-label`s as their content might be ignored or ambiguous to screen readers.
**Action:** Always verify `aria-label`s on custom keypads and icon-only buttons during implementation.
