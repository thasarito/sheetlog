## 2024-05-22 - Accessible Keypad
**Learning:** Icon-only buttons (like "Delete" in Keypad) must have `aria-label` for screen readers. Adding `active:scale-95` provides essential tactile feedback on touch devices.
**Action:** Always check `aria-label` for icon buttons and add scale transforms for touch targets.
