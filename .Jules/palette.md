## 2024-05-22 - Custom Keypad Accessibility
**Learning:** Custom interactive components like `Keypad` were built without keyboard navigation or screen reader support (missing `aria-label`, focus states).
**Action:** Audit other custom input components (like pickers) for similar omissions.
