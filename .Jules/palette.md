## 2026-02-20 - Icon-only buttons accessibility pattern
**Learning:** Icon-only buttons (like Keypad keys 'DEL') often lack `aria-label` or accessible names in this codebase, making them invisible or confusing to screen readers.
**Action:** When working on components with icon-only buttons, always verify and add `aria-label` or ensure the icon itself has an accessible name.
